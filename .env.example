# Example environment variables for local development
# Copy this file to .env and edit values as needed

# Port to serve the combined server+client
PORT=5000

# Enable the development auth shim - sets a fake user for local dev
DEV_AUTH=true
DEV_USER_ID=dev-user
DEV_USER_EMAIL=dev@example.com
DEV_USER_FIRST_NAME=Dev
DEV_USER_LAST_NAME=User

# Session secret for local sessions
SESSION_SECRET=dev-secret

# If you want to use Replit OIDC in a deployed environment, configure these:
# REPL_ID=your-repl-id
# ISSUER_URL=https://replit.com/oidc
# DATABASE_URL=postgres://user:pass@host:port/dbname
# SESSION_SECRET=your-session-secret

# AI provider keys (optional; used by some API routes)
# ANTHROPIC_API_KEY=
# ...
